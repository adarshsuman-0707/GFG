package recursion;

public class HelloWorld {
    public static void main(String[] args) {
        String ans="luffy is still joyboy";
        String arr[]=ans.split(" ");
        String last=arr[arr.length-1];
        System.out.println(last.length());
        
    
    }
}
